## 1. 数据模型与状态管理

- [x] 1.1 创建 `TabState` 类，封装单个标签页状态（笔记引用、编辑器 GlobalKey、最后访问时间）
- [x] 1.2 在 `HomeScreen` 中引入标签页列表 `List<TabState>` 和活动标签页 ID `_activeTabId`
- [x] 1.3 实现标签页的打开/查找逻辑：检查笔记是否已打开，是则切换，否则新建
- [x] 1.4 实现标签页关闭逻辑：释放资源、切换到相邻标签页、处理最后一个标签页的空状态
- [x] 1.5 实现 LRU 淘汰逻辑：超过 20 个标签页时关闭最早未使用的标签页

## 2. 标签栏 UI 组件

- [x] 2.1 创建 `TabBarWidget` 组件，水平排列标签页，支持溢出滚动
- [x] 2.2 每个标签页显示笔记标题和关闭按钮，处理标题为空时显示"无标题"
- [x] 2.3 标签页点击切换活动状态，高亮当前活动标签页
- [x] 2.4 实现标签页关闭按钮点击和中键点击关闭
- [x] 2.5 实现标签页标题随编辑内容实时更新
- [x] 2.6 实现标签页拖拽排序（Draggable/DragTarget）

## 3. 编辑器多实例管理

- [x] 3.1 使用 Offstage 包裹多个 RichTextEditor 实例，只显示活动标签页的编辑器
- [x] 3.2 每个标签页的编辑器独立维护 QuillController、滚动位置和大纲状态
- [x] 3.3 确保切换标签页时非活动编辑器不接收焦点和输入事件

## 4. 集成到 HomeScreen

- [x] 4.1 重构桌面端布局：在编辑器区域上方添加标签栏
- [x] 4.2 替换 `_selectedNote` 逻辑为标签页管理逻辑，`_selectNote` 改为打开/切换标签页
- [x] 4.3 新建笔记时在新标签页中打开
- [x] 4.4 删除笔记时同步关闭对应标签页
- [x] 4.5 确保移动端（宽度 <= 768px）不受影响，不显示标签栏

## 5. 验证

- [x] 5.1 验证打开/关闭/切换标签页功能正常
- [x] 5.2 验证标签页切换后编辑器状态（滚动位置、光标、大纲）保持不变
- [x] 5.3 验证拖拽排序标签页正常
- [x] 5.4 验证关闭标签页时自动保存生效
- [x] 5.5 验证 LRU 淘汰在超过 20 个标签页时触发
- [x] 5.6 验证移动端行为不变
